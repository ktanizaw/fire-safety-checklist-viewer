# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type ActionItem {
  id: ID!
  deficiency: String!
  proposedAction: String!
  timescale: String!
  personResponsible: String!
  priority: String!
  status: String!
}

type AssessmentItem {
  id: ID!
  question: String!
  response: String
  requiresAction: Boolean!
  lastUpdated: String
  helpText: String
  notes: String
  actionItem: ActionItem
}

type AssessmentSection {
  id: ID!
  title: String!
  description: String!
  order: Int!
  completionPercentage: Float!
  pendingActionCount: Int
  items: [AssessmentItem!]!
}

type Assessment {
  id: ID!
  buildingName: String!
  address: String!
  responsiblePerson: String!
  assessor: String
  dateOfAssessment: String
  useOfPremises: String!
  numberOfFloors: Int!
  construction: String!
  maxOccupancy: Int!
  status: String!
  overallCompletionPercentage: Float!
  lastUpdated: String!
  nextReviewDate: String
  pendingActionCount: Int
  sections: [AssessmentSection!]!
}

type Query {
  assessments(filter: AssessmentFilter, sort: AssessmentSort): [Assessment!]!
  assessmentById(id: ID!): Assessment!
}

input AssessmentFilter {
  status: String
  searchQuery: String
}

input AssessmentSort {
  overallCompletionPercentage: SortOrder
}

"""The sort order for sorting operations"""
enum SortOrder {
  ASC
  DESC
}